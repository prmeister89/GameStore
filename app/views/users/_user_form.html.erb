<%= form_for @user do |u| %>
  <%= u.label :username %>
  <%= u.text_field :username %>
  <%= u.label :name %>
  <%= u.text_field :name %>
  <%= u.submit "Submit", data: { confirm: 'Are you sure?' } %>
<% end %>
