<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>User Index</title>
  </head>
  <body>
    <h1>All User's List</h1>

    <% @users.each do |u| %>
      <li>Name: <%= link_to u.name, user_path(u) %></li>
    <% end %>

  </body>
</html>
